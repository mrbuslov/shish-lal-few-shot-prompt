name: Notify Local Server on Push

on:
  push:
    branches:
      - main

jobs:
  notify-local-server:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET request to FastAPI /update
        run: |
          curl -X GET https://profound-normally-crappie.ngrok-free.app/update
